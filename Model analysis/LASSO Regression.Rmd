---
title: "LASSO Regression"
date: "12/1/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
library(glmnet)
```

```{r}
students <- read_csv("../data/student-mat.csv")
head(students)

students %>s%
  group_by(Walc) %>%
  summarize(n=n())
```

We will proceed to fit a LASSO regression to this variable

```{r}
x1 <- model.matrix(~SEX + RACE + BORNUSA,cleaned_meps_data)[,-1]
y1 <- as.matrix(cleaned_meps_data["top_util_1_20"])
```

